This legacy requirements note was sanitized because it contained credentials
and a remote-operation URL. Do not store keys, passwords, tokens, or live
provider endpoints in the repository.

For future integrations, use a reviewed server-side service, per-user
authorization, strict allowlists, rate limits, and secrets stored only in the
provider's encrypted environment configuration.
