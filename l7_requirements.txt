This legacy requirements note was sanitized because it contained a live token
and remote-operation details. Secrets must never be kept in source files,
committed documentation, or browser code.

Use a legitimate, reviewed backend integration if one is needed in the future.
Store credentials only as encrypted server-side environment secrets, and apply
authentication, authorization, audit logging, rate limits, and an allowlist.
